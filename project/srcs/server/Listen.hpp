#ifndef LISTEN_HPP
#define LISTEN_HPP

#include <string>

class Listen
{
    public:

    private:
        std::string     _address;
        int             _port;
        std::string     _default_server;
        

};

#endif